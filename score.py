import pygame
from snake import Snake

class Score(self):
    """Handle the score count."""
    
    def __init__(self):
        """Initialize default parameters for score display."""
        self.player_score = 0
        s_font = pygame.font.Sysfont('d050000l', 30)

    def display_score():
        color = (
        s_score = s_font.render('Current Score: ' + self.snake.score, True, )
        score_rect = s_score.get_rect()
